namespace test_valid_field

enum Color {
    Red,
    Blue { value }
}

fn main() {
    let red = Color.Red
    let blue = Color.Blue { value: 255 }
    println(red)
    println(blue)
    "done"
}

// Expect
// Color.Red
// Color.Blue { value: 255 }
// done
