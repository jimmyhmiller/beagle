namespace try_function_call

fn throws_error() {
    throw("my error message")
}

fn main() {
    try {
        throws_error()
    } catch (e) {
        println(e)
    }
    "done"
}

// @beagle.core.snapshot
// my error message
