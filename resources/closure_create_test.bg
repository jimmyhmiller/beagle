namespace closure_create_test

// no-std

fn make_fn(x) {
    fn inner() {
        x
    }
    inner
}

fn main() {
    let f = make_fn(42)
    _println("created closure")
    "done"
}

// @beagle.core.snapshot
// created closure
