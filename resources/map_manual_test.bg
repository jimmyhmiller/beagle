namespace map_manual_test

fn main() {
    // Simulate what the map literal {42 100} should compile to
    let key = 42
    let val = 100
    let m = {}
    let m2 = assoc(m, key, val)
    println(length(m2))
    println(get(m2, 42))

    "done"
}

// @beagle.core.snapshot
// 1
// 100
