namespace simple_event_loop_test

use beagle.async as async
use beagle.effect as effect
use beagle.core as core

fn main() {
    // Test sleep through the implicit async handler
    println("Before sleep")
    async/sleep(50)
    println("After sleep")

    println("done")
}

// @beagle.core.snapshot
// Before sleep
// After sleep
// done
