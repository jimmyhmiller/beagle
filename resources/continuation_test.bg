namespace continuation_test

fn main() {
    // Test: invoke continuation with a value
    let result = reset {
        1 + shift(fn(k) {
            k(10)  // invoke continuation with 10, should compute 1 + 10 = 11
        })
    }
    println(result)
}

// Expect
// 11
