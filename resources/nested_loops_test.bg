namespace nested_loops_test

fn main() {
    let outer = loop {
        let inner = loop {
            break("inner")
        }
        println(inner)
        break("outer")
    }
    println(outer)
    "done"
}

// @beagle.core.snapshot
// inner
// outer
