namespace map_simple_test

fn main() {
    // Test manual map creation
    let m1 = {}
    let m2 = assoc(m1, :a, 1)
    println(get(m2, :a))

    // Test map literal
    let m3 = {:a 1}
    println(length(m3))
    println(get(m3, :a))

    "done"
}

// @beagle.core.snapshot
// 1
// 1
// 1
