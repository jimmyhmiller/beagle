namespace match_compiler_warnings_struct_test

fn main() {
    // Get diagnostics - tests if struct ID is properly written
    let diags = diagnostics()

    println("Got diagnostics")

    // Test 1: Can we access properties on the returned vector?
    let count = diags.count
    println("Diagnostic count:")
    println(count)

    "done"
}

// @beagle.core.snapshot
// Got diagnostics
// Diagnostic count:
// 0
