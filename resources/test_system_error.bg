namespace test_system_error

fn main() {
    let err = try {
        let p = Point { x: 2 }
        "should not reach"
    } catch (e) {
        e
    }

    println(err)
    "done"
}

// Expect
// SystemError.StructError { message: "Struct 'Point' not found in namespace 'test_system_error'", location: null }
