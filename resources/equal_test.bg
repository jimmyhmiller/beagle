namespace equal_test
import "beagle.primitive" as primitive

struct Thing {
    x
    y
}

fn main() {
    let x = Thing {
        x: 1
        y: 2
    }

    let y = Thing {
        x: 1
        y: 2
    }
    println(equals(x, y))
    println(equals(x, x))
    println(equals(x, 1))
    "done"
}