namespace equal_test
import "beagle.primitive" as primitive

struct Thing {
    x
    y
}

fn main() {
    let x = Thing {
        x: 1
        y: 2
    }

    let y = Thing {
        x: 1
        y: 2
    }
    println(equal(x, y))
    println(equal(x, x))
    println(equal(x, 1))
    "done"
}

// Expect
// true
// true
// false
// done