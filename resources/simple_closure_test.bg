namespace simple_closure_test

// no-std

fn make_adder(x) {
    fn add(y) {
        x + y
    }
    add
}

fn main() {
    let add5 = make_adder(5)
    _println(add5(3))
    "done"
}

// Expect
// 8
// done
