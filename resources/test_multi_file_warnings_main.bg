namespace test_multi_main

import "test_multi_a" as a

enum ColorB {
    RedB,
    GreenB,
}

fn test_b(c) {
    match c {
        ColorB.RedB => "red",
        // Missing GreenB
    }
}

fn main() {
    let diags = diagnostics()
    println("Total diagnostics from both files:")
    // println(diags)
    // TODO: Fix when structs format nested fields properly
    // otherwise I was getting inconsistent order on a persistent vector
    // It might actually be from the backed, wasn't clear
}

// Expect
// Total diagnostics from both files:
