namespace match_non_exhaustive_test

enum Color {
    red,
    green,
    blue,
}

fn main() {
    let color = Color.red

    let result = match color {
        Color.red => "RED",
        Color.green => "GREEN",
    }

    println(result)
    "done"
}

// @beagle.core.snapshot
// RED
